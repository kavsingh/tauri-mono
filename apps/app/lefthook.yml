pre-commit:
  parallel: true
  jobs:
    - run: pnpm oxlint --type-aware --type-check {staged_files}
      root: "apps/app"
      glob: "**/*.{js,ts,jsx,tsx}"
    - run: pnpm eslint {staged_files}
      root: "apps/app"
      glob: "**/*.{js,ts,jsx,tsx}"
