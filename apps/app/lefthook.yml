pre-commit:
  parallel: true
  jobs:
    - run: pnpm oxlint --type-aware --type-check --max-warnings=0 {staged_files}
      root: "apps/app"
      glob: "**/*.{js,ts,jsx,tsx}"
    - run: pnpm eslint --max-warnings=0 {staged_files}
      root: "apps/app"
      glob: "**/*.{js,ts,jsx,tsx}"
