{
	"name": "app",
	"version": "1.0.0-dev.0",
	"description": "a tauri app",
	"license": "MIT",
	"type": "module",
	"scripts": {
		"test:todo": "cd src-tauri && cargo test",
		"test": "cd src-tauri && echo 'soon: cargo test'",
		"test:ci": "vitest run --silent --coverage",
		"lint:todo": "cargo clippy --all-targets --all-features",
		"lint": "conc 'oxlint --type-aware --type-check --max-warnings=0' 'oxfmt --check'",
		"before:dev": "bun build:isolation && vite dev",
		"before:build": "bun build:isolation && rimraf ./dist && vite build",
		"dev": "tauri dev",
		"build:mac": "tauri build --target universal-apple-darwin",
		"build:win": "tauri build --target x86_64-pc-windows-msvc",
		"build:isolation": "rimraf ./dist-isolation && vite -c vite.isolation.config.ts build",
		"testscript": "bun scripts/testscript.ts"
	},
	"dependencies": {
		"@tauri-apps/api": "2.10.1",
		"@tauri-apps/plugin-log": "2.8.0",
		"shared": "workspace:*"
	},
	"devDependencies": {
		"@tauri-apps/cli": "2.10.0",
		"rolldown-vite": "7.3.1",
		"vite-tsconfig-paths": "6.0.5"
	}
}
