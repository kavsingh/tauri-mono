pre-commit:
  parallel: true
  jobs:
    - run: pnpm tsc --build --noEmit
      root: "packages/shared"
      glob: "**/*.{js,ts,jsx,tsx}"
    - run: pnpm oxlint --type-aware {staged_files}
      root: "packages/shared"
      glob: "**/*.{js,ts,jsx,tsx}"
